# Modern Layout Variant

Use this folder for future modern layout implementations that can coexist with the classic variant.
Keep shared business logic in hooks/lib and move only presentation concerns into layout folders.
